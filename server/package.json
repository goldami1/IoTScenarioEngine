{
  "name": "server",
  "version": "1.0.0",
  "description": "Local server to mock up REST api",
  "main": "index.js",
  "scripts": {
    "start": "json-server -w db.json -r routes.json"
  },
  "dependencies": {
    "json-server": "^0.12.1"
  }
}
